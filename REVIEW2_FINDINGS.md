# 第2回レビュー 統合結果（2026-07-30）

判定: **重要な修正が必要**
35エージェント・エラー0・確定27件

## 提出を阻害するもの

### BLOCK-1
【§5 L153】「残っているのは条件の内容に依存しない一様な成分であり、空間的な指定を持たない。」は実測で偽。当方が probe.py と同一条件（sd-controlnet-canny／fp16／MPS／t=500／seed0 潜在／全画素0地図）で残差そのものを取り出して測った結果 → mid 残差 RMS 0.9933 のうちチャネルごとの空間平均を引いた成分が 0.6319（63.6%）、down6 は 0.3586 中 0.1739（48.5%）。一様ではない。同じ空白地図でプロンプトだけ変えた mid 残差どうしの相関 0.943、実エッジ地図（camera）との相関 0.103。また camera 実地図では mid 3.2323 に対し空白 0.9933 で大きさ自体が 3.3 倍縮んでいるので「残差が消えるからではなく」も「厳密に0にはならない」以上の意味を持たない。★対応は2択。(A) 実測版に差し替える（上の数値をそのまま使える。当方の再現は読み込み込み40秒未満。ただし +150字前後）。(B) 3語（一様な成分／空間的な指定を持たない／残差が消えるからではなく）を落として「条件地図に由来する成分が失われる」だけに退く（字数ほぼ中立）。どちらを採っても §2 L45 の「空間的な指定を持たない一様な項に退化する」と §8 L215 の「入力依存の構造」を同時に直すこと（L45 は L43 末尾の x の記述と衝突している）。

### BLOCK-2
【§5 L153・表 L139】数値レンジの誤り。0.3846 と 0.9333 は密度0の45行の平均であって範囲の下端ではない。実測（results/probe_residual.csv、density==0 かつ source!=__empty__ の45行）→ total_rms は min 0.3608452 / max 0.4089870 / mean 0.3845762、mid_rms は min 0.8009 / max 1.0335 / mean 0.9333。同じ第5節の L145 が既に正しく 0.3608〜0.4090 と書いているので節内で数字が食い違っている。「密度 0 の地図では総残差は平均 0.3846（0.3608〜0.4090）、mid ブロックは平均 0.9333（0.8009〜1.0335）が残る」に直す。なお参照の __empty__ 行は 0.4022 / 0.9933 で、これは密度0群のどの値とも一致しない。

### BLOCK-3
【§5 L147／§9 L233／analysis.py L164-167】「参照として用意した空白の地図と**ビット単位で同一の入力**である」は事実に反する。同一なのは条件地図だけで、probe.py L76 の参照は prompt="a photo"、L64-70 は画像ごとのプロンプト（dataset.py：実画像10種＋合成共通1種＝計11種）を encode_prompt に渡している。実測でも参照 0.402186 に対し密度0の11値は 0.360845〜0.408987 で一致する値が無い（最近傍との差 3.5e-3）。撤回の判断は正しいが理由が誤り。L147 を「条件地図としては同一。timestep と潜在変数も固定しているので両者の残差の差はプロンプトの違いだけから生じる（参照側 0.4022 に対し密度0側は 0.3608〜0.4090 で一致はしていない）。密度を一切動かしていない比較を密度の効果として提示するのは誤りなので外した」に差し替え、L233 の「両者がビット単位で同一の入力であるため恒等式にすぎなかった」を「条件地図が双方とも全画素0で密度を動かしていない比較であり、近い値になっても密度については何も語らなかった」に差し替える。analysis.py L164-167 のコメントにも同じ誤りが残っているので同時に直す（コード公開しているため）。

### BLOCK-4
【§8 L215】「密度が低い入力では scale を上げても効かせる中身がない」は §4.3 の自分の測定と逆。exp2_scale.csv＋exp2b_scale_ext.csv より、最も密度の低い synth_sparse（密度 0.0081）の edge F1 は scale 1.0→1.3 で 0.333→0.877、改善幅 +0.544 が7入力中最大で、最高密度 synth_veryd（0.1316）は +0.021。§4.3 L125 が自分で「密度の低い synth_sparse では 0.333 から 0.877 へ跳ねる」と書いている。★置き換え時の注意：「条件地図が空になった入力には掛ける中身がない」と書き換えるのも不可。既定経路（controlnet.py L849-851）は空の地図の残差にも conditioning_scale を一様に掛け、その残差は 0 ではない（上記実測 mid 0.9933）。正しくは「増幅しても空間的な指定は戻らない」。また「密度と scale を同時に振った実験は行っていない」も不正確で、§6 の exp3（exp.py L188-195）は両方を同時に動かしている。書くなら「分離して振った実験は行っていない（第6節は両方を同時に動かすため分離できない）」。太字の結論文自体は最良 scale と密度の順位相関 −0.630（n=7, p=0.129）と synth_sparse の挙動に支持されるので、解釈である旨を断ったうえで残してよい。「以下は測定ではなく解釈である」を置くなら二つの節の前か「以上は」にする（現在の位置だと次段落を指す）。

### BLOCK-5
【§6 L165／exp.py L57 docstring】「目標エッジ画素率（0.045 に設定した）に当たるまで Canny の高閾値を二分探索する」は実装と違う。exp.py L57-79 の adaptive_canny は `for _ in range(iters)`（iters=12 固定）で、終了条件に目標への到達判定は無く、`abs(d - target) < abs(best - target)` で最も近い密度を返すだけ。本文を「目標エッジ画素率（0.045 に設定した）へ近づくよう Canny の高閾値を 12 回二分探索し、その間に得た中で目標に最も近い密度の地図を採る前処理」に、docstring を「目標エッジ密度に最も近づく高閾値を 12 回の二分探索で探す」に直す。コードを公開しているので docstring も直すこと。

### BLOCK-6
【§6 L178】原画像側で悪化する理由の説明が誤り。「もともと密度が 0.0647 で目標 0.045 を上回っているため」と書いているが、exp.py L189 `sc = density_aware_scale(d_ad)` が読むのは適応前ではなく適応後の密度である。exp3_improvement.csv の α=1.0 の dens_after は 0.0081/0.0130/0.0201/0.0456/0.0449/0.0452/0.0453/0.0468/0.0560/0.0630/0.0858/0.0883/0.0884/0.1316/0.1626 で、目標±10%に収まったのは5画像のみ、10画像は外れている（camera 0.0751→0.0858、immunohistochemistry 0.1884→0.1626 は目標から遠ざかっている）。「当たるまで探索」と書いたままだと scale が 0.854 まで下がる理由が説明できなくなる。因果の連鎖を「二分探索が目標に届かなかった → 高いままの適応後密度を scale 規則が読んだ → scale が下がった」に直す。到達しない理由も実測で二つ挙げられる：高閾値の探索範囲を 20〜400 に切っているため実画像では上端でも密度が落ちない／合成画像は密度が閾値に対して階段状で動かない（synth_sparse は12回の探索すべてで 0.0081）。

### BLOCK-7
【§2 L47】「制御の強さは追加ネットワークの表現力ではなく、凍結された本体へどう残差を渡すかで決まっている」は論文自身のアブレーションと逆。arXiv:2302.05543v3 §4.2 を当方で直接確認 → "The lightweight ControlNet-lite (Figure 8c) is not strong enough to interpret the conditioning images and fails in the insufficient and no prompt conditions." と、表現力の不足そのものを失敗原因として明記している。zero convolution 置換の劣化理由 "the pretrained backbone of the trainable copy is destroyed during finetuning" は、接続の設計が表現力を守っているという説明。二者択一に書くと論文の説明とも ControlNet-lite の結果とも食い違う。「論文は同時に、trainable copy を畳み込み1層に置き換えた軽量版が条件画像の解釈に力不足で失敗するとも述べている。制御の強さは追加ネットワークの表現力と、その事前学習済みの表現を壊さずに残差を渡せるかの両方で決まっており、zero convolution が担っているのは後者である。」に差し替える。この一文は §5 の主張の土台に置かれているので放置しない。

### BLOCK-8
【§2 L49／§7 L198】「論文には Limitations に相当する節が置かれていない」は、本レポートが [1] として引用している ICCV 2023 版に対しては誤り。当方が CVF 公式 supplementary（Zhang_Adding_Conditional_Control_ICCV_2023_supplemental.pdf）を取得して抽出 → "Figure 28: Limitation. When the semantic of input image is mistakenly recognized, the negative effects seem difficult to be eliminated, even if a strong prompt is provided." が実在し、Discussion 本文にも "Figure 28 shows that when the semantic interpretation is wrong, the model may have difficulty generating correct contents." とある。arXiv v3 側は当方も確認したとおり Limitations 節も "limitation" の語も無いので、括弧書きの「arXiv 版本文で確認」の範囲では正しいが、§7 L198 は範囲の限定なしに書かれている。★重要：この Figure 28 の内容は §7 L196 の「外れた予想」（プロンプトの意味内容を変えても輪郭の再現が動かない）と同じ向きの現象なので、放置すると「論文は限界を述べていない」と書いたうえでその限界と一致する観測を独自の発見として出す構図になる。§2 L49 を「なお論文本文には Limitations に相当する節が置かれていない（arXiv 版・ICCV 版本体の双方で確認）。ただし ICCV 版 supplementary には Figure 28 として限界が一点示されており、条件画像の意味が誤って認識された場合は強いプロンプトを与えても悪影響を取り除きにくいと述べられている。」に、§7 L198 の最終文を「論文本文には Limitations に相当する節が置かれておらず、ICCV 版 supplementary が ControlNet 自身の限界として明示しているのは Figure 28 の一点である。ここで挙げた限界はいずれもそれとは別に、本実験から見出したものである。」に差し替える。※§7 の6項目（指標依存・標本数・条件種類・環境固有・SD1.5限定・外れた予想）はどれも Figure 28 の限界ではないので「残りの限界は」とは書かないこと。参考文献 [1] に supplementary の所在を一行足すとより誠実。

### BLOCK-9
【§2 L43／§9 L237】RMS 0.2554・空間標準偏差 0.0012 を生成するコードがリポジトリに存在しない。`grep -rn 0.2554` の結果ヒットは REPORT.md と REVIEW_FINDINGS.md の2ファイルのみで、REVIEW_FINDINGS.md 側には「当方の実測」としてレビュー側 AI の測定であることが明記されている。`cond_embedding` を触るコードは全ファイルで0ヒット、stats_report.py も出力しない。これは §9 L237「生成AI が提示した数値をそのまま本文に載せた箇所はない」と正面から矛盾する（生成AI利用の記述は必須10項目）。★値そのものは正しい。当方が fp32/CPU で `ControlNetModel.from_pretrained(...).controlnet_cond_embedding(torch.zeros(1,3,512,512))` を実行して RMS 0.2554、絶対値最大 1.2381、チャネル平均マップの空間標準偏差 0.001163 を再現した（所要 約30秒）。対応は2択：(A) この10行程度のスクリプトを追加して stats_report.py から呼び、§9 を「レビュー過程で生成AI が提示した2つの数値については、自分で同じ測定を実装して再現を確認したうえで採用した。それ以外の数値はすべて自分の実験データから再計算している」に直す（推奨。字数もほぼ中立）。(B) §2 の当該括弧ごと落として「条件が空でも 0 を返すとは限らない」だけ残す（この主張は §5 の自前測定だけで足りる。−45字）。

## 直した方が良いもの

### SHOULD-1
【図2の注記／analysis.py L173-176】p=1.1e-43 は135回の順伝播を独立標本として計算した値。本レポートは §5 L157「全 70 点を独立標本として扱うことはできない」、§6 L167「90 行をそのまま…標本数が水増しされる」、§8 L209「全 70 点を独立標本として相関を取ることはできない」と3度明言しており、Canny 側だけ図がプールした p を出している。実測：n=135→ρ=+0.8748, p=1.12e-43／(source,total_rms) 重複除去 n=83→ρ=+0.8739, p=4.27e-27。図の注記から p を落として「Spearman ρ=+0.875（順伝播135回。重複除去83件で +0.874）」だけにするのが最短（analysis.py L174-176 の text を1行直して再実行）。相関の存在自体は重複除去後も p=4.3e-27 なので結論は変わらない。

### SHOULD-2
【§5 L135／L149】ρ=+0.875 の内訳を出していない。実測：密度>0 のみ n=90→+0.6689、その重複除去 n=68→+0.7844、密度0込み n=135→+0.8748。本文自身が L149 で「機構として意味を持つのは、密度が正の領域での減衰である」と書いているのに、その領域だけの値が本文にも図にも一度も出てこない。§4.2 L95 では同型の感度分析（+0.819→空を除いて +0.577）を明示しているので基準がそろっていない。L135 の括弧に「この +0.875 には密度 0 の 45 回が寄与しており、密度が正の 90 回に限ると +0.669、その重複を除いた 68 件では +0.784 である」を足す（+0.669 だけの1値表示は落差を誇張するので2値で書く）。あわせて L149 の「総残差は 1.1114 から 0.3846 へ単調に下がる」は正密度領域を述べる文なのに終点が密度0の値になっているので「1.1114 から 0.5161 へ単調に下がり、密度 0 まで含めれば 0.3846 に達する」に直す。★L149 後半の画像内相関（15画像すべて正、中央値 +1.000）は正密度に限る形に書き換えないこと。horse・synth_low/mid/sparse は正密度側で x がほぼ定数の縮退区間になり無意味な ρ が出る。

### SHOULD-3
【§5 L133／図2キャプション L151】「条件地図の違いだけを取り出した比較」は散布図全体には成り立たない。probe.py L64-70 は画像ごとに prompt を渡し L44 の encode_prompt を経て条件側の埋め込みになるので、固定されているのは t=500 と seed0 の潜在変数だけ。同じキャプションの直前の文が「値は 11 通りしかない（違いはプロンプトによる）」と書いており自己矛盾。厳密に成り立つのは同一画像のコントラスト系列内で、著者もそれを分かって L149 に画像内順位相関を別に出している。L133 を「全条件で timestep と潜在変数を共有しているので、これらの影響を取り除いた比較になる。系列間ではプロンプトが変わるため、条件地図の違いだけを取り出した対応のある比較が厳密に成り立つのは各画像のコントラスト系列の内部である」に、L151 の該当文を「timestep と潜在変数は全条件で固定してあるが、系列ごとにプロンプトも変わるため、密度だけを動かした比較が厳密に成り立つのは各画像の系列内である」に直す。※画像内容を交絡として並列に挙げないこと。probe.py では元画像は Canny を経た条件地図としてしか順伝播に入らないので、画像内容は条件地図そのものであって別の交絡ではない。

### SHOULD-4
【表 L139】「45（異なる地図は 11）」は誤り。密度0の45行の条件地図はすべて同じ全画素0配列なので異なる地図は1種類。11 は distinct なプロンプト数（＝total_rms のユニーク数、実測11）。2段落あとの L147 と図2キャプションが「全画素 0 の同一の地図」と明言しているので表だけが食い違い、撤回したはずの論点が表の側で復活して見える。「45（異なる値は 11。違いはプロンプト）」に直す。

### SHOULD-5
【§5 L135】「条件地図とプロンプトの組による重複を除いた 83 件」は実際の除去基準と一致しない。stats_report.py L116 は `drop_duplicates(subset=["source", "total_rms"])` で、画像ごとに残差の値が重複する行を落としているだけ。書かれた基準どおりに除去すると 79 件・ρ=+0.8584。「画像ごとに値が重複する行を除いた 83 件でも +0.874」のように実際に行った操作の名前で書く。

### SHOULD-6
【§5 L157】深度の一般化で比べている ρ が輪郭側と別種の統計量。深度 +0.696 は画像内順位相関の中央値、輪郭 +0.874 は組83件をまたいだ相関。同種で比べるなら輪郭側は L149 の中央値 +1.000。さらに実測（exp6_depth_generality.csv）で α別平均 total_rms は 0.4822（α=0）→1.3514（0.05）→1.4144→1.4850→1.5110→1.5148→1.5016（α=1.0、cond_std 平均 57.1）で、cond_std が 2.9 から 57.1 へ20倍動く区間は 1.35〜1.51 とほぼ平坦。α=0 を除いて画像内相関を取り直すと中央値 +0.514・正は 6/10（最小 −1.000）で、本文の「中央値 +0.696、9画像で正」は α=0 込みの値。つまり支えられているのは「情報がゼロか否か」の一段差だけで、輪郭で見えた段階的減衰は再現していない。太字の結論を「条件地図が情報を完全に失うと残差が縮むという対比は輪郭に固有ではない。ただし密度に対する段階的な減衰まで共通しているとは、この測定からは言えない」に弱める。

### SHOULD-7
【§6】提案した改善が1画像で条件地図を完全に空にしている（§4.2 が破綻と呼んだ状態を改善側が作った）のに本文に一言も出てこない。exp3_improvement.csv 24-26行：synth_veryd α=0.3 seed 0/1/2 で dens_before 0.131592 → dens_after 0.000000、scale_after 1.4（規則の上限に張り付き）、f1_before 0.9574/0.9358/0.8237 → f1_after すべて 0.000000、chamfer_after は NaN。原因は目標に最も近い密度を選ぶ規則が、密度が閾値に対して階段状に飛ぶ入力で目標を挟む空側を選ぶこと。本文は同じ節で「条件地図が空のときの F1=0 は指標の定義から出る値」と明記しているのに、この F1=0 は注記なしで α=0.3 の Wilcoxon に入っている（この3行を除くと p=0.0413→0.0040）。副作用として1段落追記し、adaptive_canny に「探索後の密度が 0 なら固定閾値の結果に戻す」一行の防御を書けるとなお良い。

### SHOULD-8
【§6 L176】α=0.3 の唯一の有意な結果（p=0.041）が、本文自身が「改善幅としては読めない」と断った2画像（cell Δ+0.7271、chelsea Δ+0.6678）を検定に含めたまま出ている。当方の再計算：15画像全部で n=15・12/15・+0.1194・p=0.0413／cell と chelsea を除くと n=13・10/13・+0.1141・p=0.1099。注記の直後に「なおこの2画像も α=0.3 の Wilcoxon には含めている。除くと n=13・改善 10/13・Δ中央値 +0.1141・p=0.110 となり有意水準を下回らない」を足す。★「読めない値が検定を動かしているため」とは書かないこと。Wilcoxon は差の大きさではなく符号と順位を使うので因果が違い、実際 15画像から任意の2画像を除く105通りで p の中央値は 0.0803、有意水準を下回るのは 26%（当方で全105通り計算）で、有意性が消えるのは主に標本の小ささによる。

### SHOULD-9
【§4.1 L83／§7 L192】「fp16 の表現範囲では途中で値が溢れると考えられる（上の結果と整合する説明にとどまる）」は、自分の測定が示す向きと逆。exp5_slicing.csv の fp32 は なし4.45731782 / auto4.45732069 / max4.45731163 / 1:4.45731163 / 4:4.45732069（最大差 9e-6、相対 2e-6）で、この摂動が fp16 上限 65504 に届く説明にならず、粒度非依存（slice=1 でも 4 でも NaN）は累積長が短いほど安全になるはずの溢れの予測と逆。当方が diffusers 0.31.0 の実体を確認したところ、slicing の有無で attention の実装経路そのものが入れ替わる：既定の AttnProcessor2_0（attention_processor.py L2312）は F.scaled_dot_product_attention を呼ぶが、SlicedAttnProcessor（同 L3417）は Attention.get_attention_scores（同 L547、torch.baddbmm でスコア行列を明示構成）を通る。しかも分割は系列方向ではなく batch×head 方向（`batch_size_attention` をスライス）なので各スライスの計算内容は分割数によらず同じ ＝ 粒度非依存の説明がつく。最小修正は「slicing の有無で attention の実装経路が入れ替わり（既定の SDPA から、スコア行列を明示的に構成する経路へ）、fp16 でこの経路を通ると破綻する。溢れなのか経路の演算自体の問題なのかは切り分けていない」。§7 L192 の「溢れが生じている箇所を計算グラフの中で特定したわけでもなく」も「破綻の原因を計算グラフの中で特定したわけでもなく」に直す。※L82 の「分割によって加算の順序が変わるための誤差である」は fp32 で粒度により結果が変わる事実と整合するのでそのままでよい。

### SHOULD-10
【§4.1 L83】「所要時間は fp16 で 20〜24 秒、fp32 で 88 秒だった」がリポジトリのどこにも記録が無い。exp.py の exp5()（L240-278）が CSV に書くのは dtype/slicing/nan/absmax/error の5列のみで時間計測が無く（exp.py で time を使うのは L94-100 の smoke だけ）、results/exp5_slicing.csv も5列、exp5.log にも所要時間の出力が無い。時間を出力する diag2.py は3条件しか回さず標準出力も保存されていない。§9 L237 の宣言と stats_report.py の再現性の担保から外れた唯一の数値になる。exp5() に `t=time.time()` と `sec=` を足して回し直す（1条件20秒程度）のが最善、時間が無ければ「fp32 に切り替えれば回避できるが 4 倍以上遅くなる（この時間は CSV に記録していない）」に緩める。これは字数を削れる項目でもある。

### SHOULD-11
【§4.2 L113 と図1キャプション L115】図1が直前の本文を自分のキャプションで否定している。camera のプロンプトは dataset.py L33「a photo of a photographer with a tripod」で主語は photographer。本文が「プロンプトに沿った絵が出てくる。失われているのは輪郭の指定だけである。」と書いた直後に、キャプションが「ただし人物が消え、構図は指定と無関係になっている。」と書いた図を唯一の視覚的証拠として置いている。PDF ではこの2文と図が同一視野に入る。当方で画像を確認：e1_camera_a0.15_gen.png は三脚に載ったカメラだけで人物なし（キャプションの事実記述は正しい）、e1_chelsea_a0.15_gen.png は猫、e1_rocket_a0.15_gen.png はロケットでどちらもプロンプトどおり。★本文側を直すのが正しい。「chelsea は猫、rocket はロケットというように、プロンプトの語に対応する物は画面に出る。ただしプロンプトどおりの絵になるとは限らない。図1の camera は主題である人物が落ち、三脚に載ったカメラだけが残った。生成そのものは動いているが、失われるのが輪郭の指定だけとは限らない。」の方向。※ここで CLIP 数値を持ち出すのは非推奨。CLIP は §8 L202 が初出で L127 が後方参照しており、L113 に置くと未定義の指標を先に使うことになる。また空の地図では条件が実質的に外れるので「密度0でも CLIP が下がらない」はほぼ構造上当然で、発見として出すと恒等式を発見として書く失敗の再演になる（なお実測値は 密度0の重複除去8枚 平均 0.3009・15行 0.3033、α=1.0 の15条件 0.2936、Mann–Whitney p=0.728／0.360）。

### SHOULD-12
【§1 L21】「Stable Diffusion のような規模のモデルを条件ごとに学習し直す計算資源は個人にはなく」は論文が挙げている障害ではない。当方が arXiv v3 §1 を確認 → 論文が第一に挙げるのはデータ量で "The largest datasets for various specific problems are usually about 100K in size, which is 50,000 times smaller than the LAION-5B dataset." 続けて過学習と破滅的忘却。計算量については逆に §3.2 で GPU メモリ23%・時間34%増にとどまると書いており、むしろ障害になりにくいという主張。「この論文が扱っているのは〜」の段落なので論文の問題設定として読まれる。計算資源の節は著者自身の前提として残しつつ（L25 の「学習は単一の民生向け GPU でも成立し」が受ける対比が消えるため）、「論文が障害として挙げているのはこの後者で、特定の条件について最大級のデータセットでも約10万枚、Stable Diffusion の学習に使われた LAION-5B の5万分の1しかなく、その量で直接微調整すれば過学習と破滅的忘却を招くと述べている」を足して帰属を分ける。

### SHOULD-13
【§2 L31／§1 L23】複製されるのは「エンコーダ部分」だけではない。arXiv v3 §3.2 を当方で確認 → "We use ControlNet to create a trainable copy of the 12 encoding blocks and 1 middle block of Stable Diffusion." / "The outputs are added to the 12 skip-connections and 1 middle block of the U-net." 中間ブロックはエンコーダの一部ではない。本レポートは §5 の表で mid ブロックの RMS を総残差より大きい主要数値として出しているので（probe.py の `down, mid = p.controlnet(...)`）、現在の記述だと mid の残差の出所が本文だけでは辿れない。§2 冒頭を「そのエンコーダ12ブロックと中間ブロックの複製（trainable copy）を作り」＋「複製の出力は元の U-Net の12本のスキップ接続と中間ブロックに加算される」に、§1 L23 を「そのエンコーダと中間ブロックの複製を作って条件を受け取らせ」に直す。

### SHOULD-14
【§7 L196】条件とプロンプトの競合は論文が §4.2 のアブレーションで設定(3)として既に試しており、4設定すべてで成功と報告されている（当方で arXiv v3 を確認："(3) conflicting prompts that change the semantics of conditioning images" / "ControlNet succeeds in all 4 settings."）。触れないまま「予想し、外れた」と書くと、既に論文にある結果を知らずに予想したように読める。「本文の主張には使えないが、」の直前に「なお論文もアブレーション（第4.2節）で、条件画像の意味を変える衝突プロンプトを4つのプロンプト設定の一つとして試し、4設定すべてで成功すると報告している。予想はこの報告を見落としたまま立てたものだった。ただし論文の提示はスケッチ条件での定性的な例示（無選別6枚）であり、Canny 条件で輪郭の再現を定量的に測った本結果は、その報告と整合するとは言えても、同じ測定を再現したわけではない。」を挿入する。※「再現した」「追認」とは書かないこと（論文の succeeds は定性判断で、本レポートの命題そのものではない）。冒頭の「**予想が外れた検証も残しておく。**」は変えない。直後の段落が「論文自身の主張を検証したものではない」で始まるので「正面から」を一語足して整合させる。

### SHOULD-15
【図1（fig_camera_sweep.png）】図中に焼き込んだ density と edge F1 が PDF 上で約 3.7pt になり通常の表示倍率で読めない。元 PNG は 1562×812px、make_pdf.py L130 で 158mm 幅に貼られるので 9.89px/mm、ヘッダ1行目のインクは PNG 上 13px = 1.31mm ≒ 3.7pt（本文 9.9pt／キャプション 8.8pt の半分以下）。この7個の数値は図1の唯一の数量情報で本文にも表にも出ていない（camera 単体の密度と F1 はどこにも書かれていない）。★フォント拡大は不可：ヘッダの文字塊は各列 幅191px、列ピッチ384px なので3倍にすると隣の列と完全に重なる（限界は約2倍＝7.9pt でキャプションにも届かない）。しかも図1は自動生成コードが存在せず手動合成なので締切前日の作り直しは割に合わない。キャプション L115 末尾に「密度/F1 は左から 0.0751/0.758、0.0253/0.628、0.0139/0.541、0/定義不能。」を足すのが最短。※「定義不能」は §4.2 L105 および図中の「F1 undefined」と整合する。

### SHOULD-16
【図1】左端の行ラベル cond が画像パネルに食い込んで末尾が切れている（PNG 上でパネル左辺 x=27px に対しラベルのインクは x=3〜25、下段の gen は3文字なので収まっている）。実害は小さくキャプションが「条件地図（上段）と生成結果（下段）」と明示しているので、締切優先ならこのままでも読解は妨げない。再合成するなら左余白を 27px から 80px 程度に広げるか行ラベルを消す。

### SHOULD-17
【図2の軸ラベルと縦点線／analysis.py L157, L163, L169】(a) 軸ラベルだけ「条件マップ」で、キャプション・本文・表はすべて「条件地図」に統一されている（REPORT.md L91・L97・L137・L151）。(b) ×の横位置は analysis.py L157 `zx = pos["density"].min()/3` の決め打ちで、対数軸上の隙間に意味は無い。軸を切断した記号がなく、引かれているのは L163 の説明のない縦点線1本だけなので閾値や境界として読む余地がある。軸ラベルを「条件地図のエッジ画素率（対数）」に直し、キャプションに「点線より左は、対数軸に載らない画素率0を別枠で置いたもので、横位置そのものに意味はない」を一文加える。

## 論文引用の総括

論文引用は、当方が arXiv:2302.05543v3 本文と CVF の ICCV 2023 公式 supplementary を直接取得して逐語照合した。★白だったもの（確認済み・触る必要なし）：(1) §2 L33 の式 y_c = F(x;Θ) + Z(F(x + Z(c;Θ_z1);Θ_c);Θ_z2) と Z が 1×1・重みバイアス0初期化であること、学習開始時に y_c = y になること。(2) §2 L35 の zero convolution 採用理由（有害な雑音が微調整に影響しないよう 0 から徐々に育てる）。(3) §2 L37 の解像度依存の重み w_i=64/h_i が classifier-free guidance の特定の場合の手当てであること、および diffusers 0.31.0 の guess_mode が torch.logspace(-1,0,...) で対応していること — 前回の重大な誤指摘（実装を批判した件）は正しく撤回され、現在の記述は論文・実装の双方と一致している。ここは白。(4) §2 L41 の「プロンプトの 50% を空文字に置換」とその狙い（条件画像の意味を直接認識する能力を高める）。(5) §1 L25 の「1万ステップ足らずで急に成功」— supplementary の sudden convergence（1e-5 で 5000〜10000 ステップ）と一致。(6) 書誌情報 ICCV 2023 pp.3813-3824 / DOI 10.1109/ICCV51070.2023.00355 は正しく、§9 L229 の CVPR→ICCV 訂正の記述も正しい。★誤りは5件で、内訳は blocking が2件（§2 L47 のアブレーション解釈の逆転、§2 L49・§7 L198 の Limitations 不在）、should_fix が3件（§1 L21 の障害の帰属をデータ量ではなく計算資源にしている、§2 L31・§1 L23 の複製対象から中間ブロックが落ちている、§7 L196 が論文 §4.2 の設定(3)を見落としている）。いずれも「論文に無いことを論文の主張として書いた」型ではなく「論文にあることを見落とした／論文の説明と逆に要約した」型である。捏造引用はゼロ。

## 図の総括

図2枚とも当方で画像を直接開いて確認した。★図2（fig4_residual.png）— 前回の重大な指摘であった「完全に空白のマップでの残差」の赤い水平基準線は完全に消えており、撤回した比較を図が視覚的に主張する状態は解消されている。ここは白。散布図の点の配置、左端×の n=45、凡例、タイトルの主張（密度が下がると残差が縮む）はいずれもデータと整合する。残る問題は3点で、内容の誤りではなく (a) 注記の p=1.1e-43 が本文3箇所で禁じている標本数の水増しをそのまま出していること、(b) キャプション内で「違いはプロンプトによる」と書いた直後に「条件地図の違いだけを取り出した比較」と述べて自己矛盾していること、(c) 軸ラベルの用語ゆれ（条件マップ／条件地図）と説明のない縦点線。★図1（fig_camera_sweep.png）— キャプションの事実記述はすべて画像と一致していた。α=0.15 列は density=0.00000 が焼かれており「F1 undefined」も §4.2 L105 の扱いと整合、生成画像は破綻しておらず、「人物が消え」も実際そのとおり（e1_camera_a0.15_gen.png は三脚に載ったカメラのみ）。図そのものに誤りは無い。問題は図と本文の関係で、直前の L113「プロンプトに沿った絵が出てくる。失われているのは輪郭の指定だけである。」を図のキャプションが否定しており、しかも他の密度0の例（chelsea→猫、rocket→ロケット）はプロンプトどおりなのに数少ない例外を図に採用している。直すべきは図ではなく本文 L113。加えて焼き込み文字が PDF 上 3.7pt で読めない件と cond ラベルの切れがあるが、どちらも体裁の問題。★図の生成コード側：analysis.py L164-167 のコメントに、本文 L147 と同じ「ビット単位で同一の入力」という誤った理由づけが残っている。赤い基準線を消した判断は正しいのに、消した理由の記述が誤ったまま公開リポジトリに残っている状態なので、本文と同時に直すこと。

## ページ配分

★字数の余白は実質ゼロ。当方が REPORT.md をスクラッチに複製して make_pdf.py を実測した結果、現行のままなら 8ページ（抽出16,883字、再現確認済み）だが、§4.2 の本文段落に 20 字を足すと 8ページ、40 字を足すと 9ページになり make_pdf.py の verify() が終了コード1を返す。つまり「本文に追加できるのは 20〜40 字」。挿入位置によって多少変わり、キャプション（CAP 8.8pt）への挿入なら 58 字までは 8ページに収まる。削減と追加はほぼ 1:1 で相殺されるので、**純増を +20 字以内に収める**のが運用ルールになる。★blocking を全部直すと概算で +500 字前後（§5 L153 の実測版で +150、§2 L49＋§7 L198 で +140、§2 L47 で +75、§6 L165/L178 で +60〜+180、§8 L215 で +30〜+120、§9 L237 で +70、§2 L43 の括弧を落とせば −45）。should_fix まで入れると +1,000 字を超える。以下の削り先を上から順に使うこと（いずれも当方が実際に削って make_pdf.py を通し、8ページ維持を確認済み）。① **L19 を段落ごと削除（58字＋空行）**＝「採録は Crossref・DBLP・Semantic Scholar の3つで独立に確認した（詳細は参考文献末）。」は L251 に完全に包含されている純粋な重複。段落が1つ減るぶん行間も浮くので、実測で約 100〜170 字ぶんの余白を生む。最優先。② **L23 の末尾3文（約105字）**＝「0 で初期化されているので学習開始時点では複製の寄与が完全に 0 であり、元のモデルの出力と一字一句一致する。そこから徐々に条件の効き方を学ばせる。凍結された本体が壊れないことが構造で保証されている点が要点である。」は §2 L35 が同じ内容をより正確に書いている。§1 は概要なので「複製の出力を zero convolution を通して残差として加える」までで足りる。③ **L159 の飽和の段落（200字）**＝§5 で最も load-bearing でない（本レポートの主張の連鎖に入っていない）。惜しいが、①②③の3つで実測 約300〜450字の追加余地が生まれ、blocking 全件を賄える。④ さらに要るなら **L83 の「所要時間は fp16 で 20〜24 秒、fp32 で 88 秒だった。」（約30字）を削る**＝should_fix の「時間が未記録」の指摘が同時に解消するので二重に得。⑤ **L25 の第2文（約60字。単一GPU・sudden convergence）**＝本文で二度と参照しない背景情報。⑥ **L127（183字）の後半**＝§8 L211・L219 とほぼ同趣旨の再掲。★逆に削ってはいけない箇所：L105（指標が定義から0を返すという撤回の説明）、L109-111（8枚の重複除去と偶然一致水準）、L145（順伝播回数は独立標本ではない）、L167（検定単位の是正）、L186-198（Limitations は必須10項目）、L219 後半（既定値と指標の関係＝考察の結論）。★作業順：REPORT.md を直す → exp.py L57 docstring と analysis.py L164-167／L174-176 も直す → `/usr/bin/python3 make_pdf.py` を実行し「ページ数: 8」「欠字: なし」を確認 → 抽出テキストに「ビット単位で同一の入力」「空間的な指定を持たない」「当たるまで」「効かせる中身がない」が残っていないことを grep で確認 → CHECKLIST.md の字数とページ数を更新。図2の注記から p を落とす場合は analysis.py を再実行して PNG を作り直してから make_pdf.py を回すこと。
