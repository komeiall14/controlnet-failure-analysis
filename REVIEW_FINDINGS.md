# 敵対的レビューの指摘（2026-07-30 / 61エージェント・反証検証済み）
確定 37件 / 反証で消えた 17件

## 必須修正

### MUST-1
【必須項目2の欠落】§1 に論文概要の本文がない。L21 は `<!-- TODO 概要本文…-->` のままで、make_pdf.py L100 が `<!--` 始まりの行を捨てるため、現行PDF（4ページ）の第1節は書誌情報と3ソース採録確認だけになっている。(a)解いた問題＝事前学習済みT2Iに空間条件を後付けする／少量データでも本体を壊さない、(b)手段＝凍結本体＋trainable copy＋zero convolution、(c)一般性＝Canny/depth/pose/segmentation を同一枠組みで扱う、(d)評価と主張の骨格、の4点を arXiv 版本文で確認した範囲で書く。第2節は機構の詳細に絞り、重複させない。

### MUST-2
【要件「他者が使える形にする」の成果物が不在】L9 が「（GitHub URL を記載）」のプレースホルダで、リポジトリは `git log` が「does not have any commits yet」、`git remote -v` も空。exp3 の完走を待たずに先にコミットして push する。.gitignore が `results/images/` を除外しているのは妥当（CSV・ログ・figs は残す）。README に環境（M1/MPS/torch 2.2.2/diffusers 0.31.0）、run_all.sh の各段と .done マーカーの意味、attention slicing を無効にしている理由、LICENSE_DATA.md への導線を書き、L9 を実URLに置き換える。LICENSE_DATA.md がリポジトリ内にしかないため、URL がないと禁止項目「著作権違反データ利用」への適合をPDF単体で確認できない点も要件上の問題。

### MUST-3
【§4.2 最重要項目の第一の論点が循環している】pipeline.py L74-82 の edge_f1 は条件エッジが空だと `rec = tp_r/max(c.sum(),1) = 0`、`prec = (g&cond_d).sum()/max(g.sum(),1) = 0` となり、生成画像が何であっても f1 が恒等的に 0 になる。したがって L92 の「edge F1 は例外なく 0.000、標準偏差も 0 であり、確率的な失敗ではなく決定的な破綻である」は破綻の測定ではない。3点直す。(a) 決定性の帰属を閾値処理に移す（同じ入力と同じ閾値なら常に空の地図が出るのでシードに依存しない）。(b) 指標の退化を自分から開示する（適合率は空虚に0、再現率は 0/0 を max(c.sum(),1) で0に落としている。chamfer が NaN なのも同じ理由で、表から除いている旨も同時に片付く）。(c) 差し替え証拠を出力側に置く。当方で pipeline.edge_f1 を使って実測した：密度0の生成画像15枚を、同じ入力の α=1.0 の条件地図（本来指定したかった輪郭）と照合すると F1 平均 0.2391（中央値 0.2198、sd 0.1850、範囲 0.0144〜0.6570）。正しく条件づけた α=1.0 の生成は同一指標で 0.7329（n=15）。別入力どうしを組み合わせた偶然一致の床は 0.2461（n=210）で、密度0の 0.2391 はこの床と区別できない（Mann-Whitney p=0.807）。生成側エッジ画素率も 0.0074〜0.2539 で出力は普通の画像である。あわせて、15行が独立な15観測ではなく独立生成は8枚である（synth 5行・cell 3行・chelsea 2行が md5 一致）ことを明記する。表 L86 の密度0行の F1 三列は 0.000 ではなく「—」にし、脚注で定義上0に固定される旨を書く。L82 の相関も ρ=+0.819（n=60）と、定義上0の15条件を除いた ρ=+0.577（n=45、p=3.4×10⁻⁵）を併記し、前者が押し上げられていることを断る。

### MUST-4
【§5 の「決定的」な比較が恒等式】L125 の「エッジが消えた条件地図が注入する残差は 0.3846 で、何も描かれていない地図を与えたときの 0.4022 とほぼ一致する（差 0.0176、4.4%）」は、密度0＝Canny 出力が全画素0＝参照行の `np.zeros((512,512))` とビット単位で同一の入力であり、probe.py は seed=0・t=500 も固定しているので、一致は測定前から確定している。「決定的」と「4.4%」は撤回する。書くべきは、(a) 同一入力であることを先に明かす、(b) 密度0の45行が取る値は distinct 11個しかなく、それが11種類のプロンプトと一対一に対応する（0.3608〜0.4090、SD 0.0150）、参照行の 0.4022 はその範囲の内側で、強調していた差 0.0176 はこの散らばりの1.17倍にすぎない、(c) 参照行は n=1 でプロンプトのみ "a photo"（他行と異なる）。「そもそも条件を与えないこと」という言い方も撤回する（空の条件地図と ControlNet 不在の混同。真の不在は残差が厳密に0）。機構の芯は密度が正の領域の減衰に移す（総残差 1.1114→0.3846 の単調低下、実画像10点のうち9点で画像内の密度との順位相関が +0.98 以上）。

### MUST-5
【§5 L127 と §2 L37 の三段論法が実測と論文の両方に反する】「Z(c; Θ_z1) が消えることで第2項全体が縮み、y_c が無条件生成 y に一致している」は三段すべて通らない。(a) 学習後の条件エンコーダは全0を入れても0を返さない（当方の実測：sd-controlnet-canny の実重みで出力 RMS 0.2554、絶対値最大 1.2381。ただしチャネル平均マップの空間標準偏差は 0.0012 で、空間構造をほぼ持たない一様な場）。(b) 仮に条件側が0でも第2項は Z(F(x+…;Θ_c);Θ_z2) なので、潜在変数 x が trainable copy を通る成分が残る。両 Z が0を返すのは論文が書いているとおり学習開始時点だけである。(c) レポート自身の測定が非消失を示している（空白地図で総残差 0.4022、mid 0.9933）。しかも y = F(x;Θ) はテキストは与えたまま空間条件だけが無い出力で、「無条件生成」ではない。書き換えの向きは「消える／一致する」ではなく「入力依存成分が失われ、空間情報を持たない定数項に退化する」。§2 L37 の「c が情報を持たなければ第2項全体が縮み、y_c は無条件生成 y へ漸近する」も同じ理由で直す（第2項が0にならないこと、失われるのは輪郭の空間的指定であることを明記）。y_c と y の一致は測っていないので書かない（scale=0 を1点足せば測れるが、締切と生成コストを考えれば主張を落とす方を勧める）。同じ段落末尾の「相関の観察ではなく、論文が与えた式から予測される挙動をそのまま測定したことになる」も撤回する。式(2) が予測するのは初期化時に y_c=y になることだけで、学習後に残差ノルムがエッジ画素率に対して単調増加することは式から出てこない。単調性は測定結果として提示する。

### MUST-6
【§5 の標本数と測定条件】(a) L114 の n=135・p=1.12×10⁻⁴³ は、136行中56行が重複値（distinct 79〜83）で、同一条件地図の行は決定論的な順伝播なので数値までビット一致する。主値を ρ=+0.874（n=83）とし、p 値の桁を前面に出さない。(b) 表 L116 の n 列は独立観測数ではなく順伝播回数である旨を注記し、密度0行は「45（独立な順伝播は11）」と書く。(c) L112 に測定条件を明記する：t=500、固定した潜在変数（seed 0）、条件側の枝のみ（CFG の無条件側は測っていない）。全条件で t と潜在変数を共有しているので対応のある比較であること、拡散を回さないので1条件1秒未満で済むことも同時に書ける。

### MUST-7
【§4.2 第二・第三の論点が自表と矛盾している】(a) L96「標準偏差が密度とともに単調に縮んでいる」は表の 0.000/0.229/0.288/0.145/0.088 と食い違う（第2帯→第3帯で増加）。ばらつきの山が 0.005〜0.02 の帯、つまり崩壊の境目にあることを書く方が情報量が多く、後続段落（0.0201 が最大、0.0016 の小ささは見かけ）と噛み合う。密度0帯の 0.000 は L92 で述べた決定性の反映なので傾向の系列に混ぜない。(b) 同 L96 の「条件間の標準偏差 0.3852」は expvar（5条件×5シード）から出ない。0.3852 は exp1 の全60行の f1 の標準偏差で、母集団が違う。expvar 内で揃えるなら条件平均の標準偏差 0.3543（比は約5分の1）。exp1 側の広がりに触れたい場合は「第4.2節の60条件全体の標準偏差 0.3852、F1 が定義上0になる15条件を除くと 0.2938」と母集団と内訳を明記する。(c) 帯別SDの中身はほぼ入力画像の違いである（同一画像内の成分は0.004以下、(0,0.005] 帯は6枚から1行ずつなので全量が画像間差）ので、「不安定」という語は帯別SDではなく expvar が支える場所でだけ使う。(d) L94「密度 0.005 を下回る帯では平均 0.109 に対し中央値が 0.012 で、分布が両端に割れている」は n=6 のうち5件が 0.054 以下、rocket の1件だけが 0.574 で、乖離はこの1点で全部説明できる。二峰性は主張できないので内訳を書いて「帯の条件数からは分布の形まで言えない」に留める（chelsea は 0.0539 なので「0.05以下」と書くと誤り。「0.054 以下」とする）。(e) 同 L94「0.05 を超えると 0.841 で頭打ちになる」は削除する。0.05超が最上帯で比較相手がなく、帯平均の増分は +0.559→+0.053→+0.120 で最後の一歩がその前より大きい。飽和ではなく「ばらつきが急速に縮む（0.288→0.145→0.088）＝制御が当てになる領域」と書けば、頭打ちを主張せずに同じ実務的含意が残る。これに伴い L129 の「第4.2節で edge F1 が 0.05 付近から頭打ちになったのと対応しており」も直す（残差側の飽和は事実なので残し、内部で先に飽和が起きているのに出力側にはまだ見えていない非対称として書く方が観測に忠実）。

### MUST-8
【§3 実行環境の3値が実測と合わない】(a) L52「実測 1枚あたり 120〜195秒」→ sec 列を持つ全CSV 129件で min 101.3／max 184.1／中央値 106.8／平均 112.9。120秒未満が101件（78.3%）、195秒はログ全体に1件も存在しない。「20ステップ・512×512 で実測 101〜184 秒、exp1 の60枚では平均 119 秒」のように範囲と出典を書く。あわせて §4.1 の切り分け表は DDIM 4ステップで別条件である旨が分かるようにする。(b) L50「fp16、計約3.6GB」→ 実重みは unet 859.521M＋vae 83.654M＋text_encoder 123.061M＋controlnet 361.279M＝1,427.515M パラメータで fp16 換算 2.66GiB。ディスク上の fp32 実体は 5.32GiB。どちらの読み方でも 3.6GB にならない。「fp16 で約2.7GiB（重み計 1.43×10⁹ パラメータ、配布重みは fp32 で 5.3GiB）」に直す。(c) L47「macOS 14.4」→ 実機は 14.4.1（BuildVersion 23E224、当方で sw_vers 確認）。MPS の NaN は OS とドライバの版に依存する報告なので版数は正確に書く。

### MUST-9
【§2 L31 の論文帰属が適用条件を落としている】`w_i = 64/h_i` は 3.4節 Inference の Classifier-free guidance resolution weighting の中で、条件画像を ε_c 側だけに足す場合の手当てとして導入されており、常に各接続へ掛けるとは書かれていない。diffusers の guess_mode はその条件に対応するフラグで、logspace(-1,0,13) は 64/h_i を最大値1に正規化した対数平滑版（深いブロックほど強いという向きと約8倍の幅が一致）。公式実装も同じ重みを guess mode に限定している（`strength * 0.825**(12-i)`、約0.099〜1.0）ので、既定で一様なのは移植による欠落ではない。したがって L31 末尾の「論文の式を読んで実装を使うと、解像度依存の重みが既定で効いていると誤解しうる」は、実装に存在しない齟齬を実装のせいにしているので削除する。CFG が打ち消されるのは条件画像を ε_uc と ε_c の両方に足したときであり、「プロンプトが無いと CFG が消える」と書くと新しい誤りが入るので注意。1,1,1,2,2,2,4,4,4,8,8,8,8 という具体値を出すなら、論文の添字は h_1=8 が最も深い側で diffusers の並びと逆順なので「浅い側から並べると」と明記する。値を出さず「深いブロックほど強い」だけでも論旨は保てる。

### MUST-10
【§6 を書く前に検定設計を直す】analysis.py L110-111 が `stats.wilcoxon(df["f1_after"], df["f1_before"])` を全行に対して呼んでいる。exp3 は1画像あたり2コントラスト×3シードの6行を作る（exp.py L184-207）ので、n がおよそ6倍に膨らみ対の独立性が成り立たない。§4.2・§5 と同じ誤りを改善の検証で繰り返すことになる。α で層別し、各層の内部で画像を単位として3シードを平均してから Wilcoxon をかける。α を混ぜてはいけない：実行途中のデータで低コントラスト側は改善（画像単位 8/12、Δ中央値 +0.077、p=0.27）だが原画像側は悪化（3/11、Δ中央値 −0.078、p=0.12）で符号が逆であり、混ぜると打ち消し合って「何も言えない」結果になる。n=12 では検出力が低いので p 値に寄りかからず、改善した画像数・Δ中央値・α別の符号の違いを主に述べ、どちらの層も有意でないことを明記する。数値は exp3 完走後の CSV で確定させる（現在 69/90 行、page が3/6。残り21行×2生成で完走は正午前後の見込み。※この所要時間は現在の進行速度からの推定）。

### MUST-11
【図が0枚。貼るなら fig2 の欠陥も同時に直す】映像メディア学の課題で生成画像も条件地図も1枚も示していない（REPORT.md に `![` が0件）。素材は results/figs/ に3枚、results/images/ に378枚あり、make_pdf.py L124-133 に埋め込み機構が実装済み、現行PDFは4ページで上限8ページに余裕がある。(a) 最優先の1枚は camera の α スイープを2段×4列（上段=条件地図、下段=生成、α=1.0/0.5/0.3/0.15）で1枚に合成したもの。camera の α=0.15 は条件地図が実際に全黒（mean 0.0/max 0、α=1.0 は mean 19.15）なので主張と画像が一致する。astronaut の α=0.15 は mean 0.0175/max 255 で全黒ではないので使わない。(b) fig4_residual.png はそのまま（112mm）。(c) exp3 完走後に fig3_improvement を生成して §6 に。(d) make_pdf.py L130 は `w = min(A4[0]-40mm, 165mm)` で常に横165mmへ拡大し、横並び機能は無いので、複数枚は PIL で事前に1枚のワイド画像へ合成する（4枚を2048×512 に並べれば 165×41mm）。512×512 を単独で貼ると1枚で本文段の64%を食う。(e) fig2_scale.png は現状「Spearman ρ=+nan」と焼き込まれており（best_scale が7件すべて1.3で定数入力）、しかも左図題「最適な scale は入力ごとに異なる」・右図題「密度と scale は同じ資源の二つの支払い方になっている」が本文L102の結論と図の見た目の両方に反する。貼るなら analysis.py L94-98 の `if len(b) > 2:` を `if b["best_scale"].nunique() > 1:` に変え（exp2b が完走すれば定数でなくなる可能性があるので分岐にする）、左図題は「既定値 1.0 が最良の入力は7件中0件。最良は全件が掃引上限の 1.3」、右パネルは密度 vs best_scale を捨てて Δ(1.0→1.3) を密度昇順に並べた棒グラフ＋題「改善幅は入力によって大きく異なる（+0.020〜+0.544）」にする（密度と改善幅の相関は ρ=−0.393、p=0.383 で無いので密度順の傾向を題にしてはいけない）。本文の図番号はファイル名の番号（fig1/fig2/fig4）と一致しないので登場順で振り直す。

## 直した方が良い

### SHOU-1
地の文の太字9箇所を全廃する（L35, L37, L92, L96, L102, L104, L125）。表セル内の2箇所（L70, L123）は残してよい。L102「**この予想は外れた。**」は一文全体が太字で、学術文書の体裁から外れる。強調は語順で作る：段落先頭に「この予想は外れた。」を移し、その後に当初の予想と結果を続ける。L104 は現行文のアスタリスクを外すだけでよい（提案されていた「7件すべてで1.3が上回った」への書き換えは L102 とほぼ同内容の重複になり、4.3の見出し「既定値は構造整合に対して弱すぎる」との呼応も崩れるので採らない）。4.2 の三点並列は第一・第三だけ強調され第二が素という不整合が太字の無機能さの実例なので、三点すべて素の地の文にする。

### SHOU-2
§4.2 冒頭に edge F1 の定義を1文入れる。本文に定義が一切なく、同じ量を「構造整合」と「edge F1」の二通りで呼んでいる（L62, L82, L84, L100, L106）。例：「構造整合は、生成画像から条件と同一設定の Canny（閾値 100, 200）でエッジを再抽出し、条件地図との一致を 2px の膨張を許した precision と recall の調和平均で測った値である（以下 edge F1）。」以降は呼称を統一する。あわせて指標族としての先行に短く帰属させる：輪郭条件の制御性を再抽出エッジの F1 で測る枠組みは ControlNet++（arXiv:2404.07987、評価用 Canny 閾値も (100,200) で一致）が用いており、数px の許容を入れる操作も arXiv:2403.00467（σ=2 の max-pooling）にある。ただし前者は許容幅を明示せず、後者は条件側 recall のみの片側指標なので、双方を膨張させて F1 にする本レポートの形はいずれとも同一ではない、と明記する。この一文が、外部の F1 数値と直接比較されることを防ぐ。

### SHOU-3
§7 Limitations の「edge F1 は許容2pxに依存する」を、実測で強化する。許容幅のみを振ると高密度帯（n=14）の平均は 0px で 0.298、1px 0.728、2px 0.829、3px 0.869 と動く。密度に対する単調性と 0.05 付近の挙動という結論はいずれの許容幅でも保たれるが、絶対値は指標設計に強く依存するので他文献の F1 と直接比較はできない、と書く。数値は pipeline.edge_f1 の tol を引数で振って in-pipeline で出し直すこと（保存PNGからの再計算では e1_immunohistochemistry_a1.0 で CSV 0.8496 と再計算 0.7275 の 0.122 の乖離が1件ある。レポートに載せる数字は同一経路で生成する）。

### SHOU-4
§4.1 の3行表（24/20/88秒、absmax 3.38/4.46）だけが results/ 配下に照合できる保存物を持たない。diag2.py は標準出力に印字するだけで results/ に diag2 のログが無い。既に実装済みで run_all.sh L46 に組み込まれている exp5（10条件で nan と absmax を記録し exp5_slicing.csv と exp5.log を残す）を走らせ、3行の場当たり表より強い証拠に置き換える。所要秒も残したいなら exp5 の rows に sec を1項目足す。注意：diag2.py を再実行して表を更新しないままログを添えると、ログと本文が食い違い照合できない状態より悪くなる。ログを取るなら表の数値は同一実行のものに差し替える。秒を測り直さない選択をするなら、主張（NaNで静かに壊れる）に対して従属的なので所要列自体を落としてよい。本文には出典表記が1つも無いので、表ごとにパスを貼るのではなく L9 のソースコード欄か第3節に「全数値は results/ 配下のCSVとログから再現できる」と一括で示す方が体裁と整合する。

### SHOU-5
§2 L31 の最終文を削除して同一命題の3回反復を解く（L31 第2文「既定では働いていない」／同第4文「既定で効いていると誤解しうる」／L33 第1文「一様な係数が掛かっているだけ」）。L33 は統合せずそのまま残す。第5節の段間比較の妥当性はこの申告に依存しているため、一般条件文に置き換えると情報が消える。ただし「本レポートの実験はすべて既定設定で行っているので」は §4.3 で conditioning_scale を 0.2〜1.3 に振っている事実と表面上ぶつかって見えるので、「すべて guess_mode=False の既定経路で行っているので」と経路を特定した書き方にする（一様係数であることは scale の値に依らず成立する）。

### SHOU-6
§4.3 L106「掃引範囲を狭く取ったために単調増加しか観測できず」を緩める。7系列中2系列が非単調で、synth_sparse は scale 0.6→0.8 で 0.320→0.318、synth_veryd は 0.2→0.4 で 0.308→0.278 と下がる。下げ幅は小さく本筋は変わらないが、過大表現を避けるルールに従うなら「ほぼ単調な増加しか観測できず」等に留める。

### SHOU-7
L98 の「条件内の標準偏差そのものも密度に依存していた」を範囲限定に直す。5条件を密度順に並べると 0.0517（0.0016）／0.1433（0.0201）／0.0544（0.0586）／0.0512（0.0884）／0.0213（0.1884）で単調ではなく、単調なのは 0.02 を超える4条件（0.1433→0.0544→0.0512→0.0213）。既にある床効果の断り書きは残す。検定は無力（条件内SD と密度の Spearman ρ=−0.70、p=0.188、n=5／Levene p=0.164）なので、有意でない p は本文に置かず Limitations に一文だけ入れる（「方向が一貫するにとどまり検出力はない」）。

### SHOU-8
§5 に残差の水準を測る基準がないので1文足す。同一条件（t=500、同一 latent、プロンプト "a photo"）で残差の加算先である凍結 UNet の skip 特徴を測ると総RMS は 1.349 で、密度0での残差 0.402 はその約30%、高密度側は約77%にあたる。摂動自体は残るので無条件生成と同一の画像になるとは言えず、失われているのは c への依存性である、という形で書く。宿主の skip 特徴は条件地図に一切依存しない（latent・t・プロンプトだけで決まる）ので、表に行ごとの比の列を足すのではなく単一の参照スケールとして注記1行に置く。測定スクリプトは /private/tmp/claude-501/-Users-allcontrol-------------------/57820ab4-c8d2-43e2-8a9a-90282b00737e/scratchpad/host_ratio.py にある（unet.conv_in／各 down_block／mid_block への forward hook 10行程度）。数値を本文に書くなら再現手段をリポジトリに含める。

### SHOU-9
密度帯の境界（0/0.005/0.02/0.05）がリポジトリのどのスクリプトにも存在しない（analysis.py も exp.py も帯を計算しない）。帯集計を analysis.py に関数として入れ、CSV から表が再生成できるようにする。境界をコード側に置けば出所の問題も同時に解ける。あわせて、帯は記述用に手で切ったこと、実質的に非恣意的な境界は密度がちょうど0（n=15）だけであること、単調性の主張は帯ではなく連続変数上の Spearman に依っていることを一文添える。等頻度分位への切り直しは、密度0の15条件を微小非零と混ぜてしまい第4.2節の最も強い観察を壊すので採らない。

### SHOU-10
§5 の残差が密度の関数ではなく配置に依存する点を書く。点灯画素数を揃えて配置だけを変えると残差は3倍以上動き、密度が低い方が大きな残差を出す場合もある（同一プロンプト・t=500・seed 0 での実測：astronaut の canny 地図 密度0.0900 で 1.5552／同じ画素を全面にシャッフル 密度0.0900 で 1.1338／同じ画素数を左上に集約 密度0.0893 で 0.4717／右半分を消しただけ 密度0.0557 で 1.2143）。したがって ρ=+0.874 は密度と「エッジが画面をどれだけ覆っているか」を混ぜて拾っている。密度を横軸に使い続けるなら、シャッフルと局在の対照を数点足して残差が画素数ではなく分布に効いていることを明記するか、少なくとも「密度単独の効果」と断定しない。1条件1秒未満なので追加コストはほぼゼロ。

### SHOU-11
exp2b（scale 1.6/2.0/2.5/3.0 の7入力＝28生成）の完走を確認する。run_all.sh は exp3 の後に exp2b を queue しているので、exp3 完走後およそ50分の見込み（※現在の生成速度からの推定）。§4.3 L106 は「第6節では範囲を 3.0 まで延ばし、プロンプト追従（CLIP 類似度）と併せて評価する」と約束しているので、間に合わなければこの文を書き換え、Limitations に「最適 scale は右側打ち切りで、1.3 以上であることしか確定していない」を明記する。また §8 で「密度と scale が同じ資源の二つの支払い方」を主張するなら、その根拠を exp2 の密度相関に置けない（密度と Δ(1.0→1.3) は ρ=−0.393、p=0.383）。根拠は §4.2（ρ=+0.819, n=60）と §5（残差測定）側に置く。exp3 が走っている間に別の生成プロセスを並行させないこと。M1／16GB で MPS を競合させるとメモリ圧迫と所要時間の汚染（exp5 の秒数が意味を失う）を招く。

### SHOU-12
PDF を最後に必ず再生成する。現行の 48266454_新井滉明_映像メディア学_レポート.pdf は 07:12 のビルドで REPORT.md（10:03）より古く、第1節の概要欠落もそのまま出力されている。生成後に (a) ページ数が4〜8に収まっているか（図を入れると膨らむ）、(b) 図が本文の該当節の近くに落ちているか、(c) make_pdf.py の verify() が拾う `**` の変換漏れが無いか、(d) 図の軸ラベル・図題と本文の用語・結論が一致しているか、を確認する。

## 任意

### OPTI-1
本文の「地図」14箇所（L29, L35, L37×2, L78見出し, L80×2, L84表頭, L92, L112, L116表頭, L123, L125×2）を「マップ」に寄せる。画像処理で map は「マップ」で、「地図」は機械翻訳語として読み手に伝わる。現状はコード・図側が「条件マップ」（analysis.py L48/L91/L168、pipeline.py L48/L56、exp.py L3、probe.py L1/L76）なので、図を貼るなら同一PDF内で同じ対象が二つの名前で呼ばれることになる。図を貼らないなら現行PDFに衝突はないので優先度は最後でよい。一括置換は不可：exp.py L244・run_all.sh L46 の「地図化」は「網羅的に調べる」意味の動詞用法なので「発生条件を掃引する」等に言い換える。L123 の表セルは列見出しと重複するので「（参照）完全に空白」で足りる。

### OPTI-2
「そのもの」6箇所（L35, L37, L64, L98, L108見出し, L110）のうち L35 と L98 を落とす（L35「テキストが無いときには条件地図から意味を読み取るよう仕向けられている」、L98「一方で条件内の標準偏差も密度に依存していた」）。L37/L64/L108/L110 は相関構文で、外すと弁別が潰れるので触らない。特に L110「生成そのものが難しくなった」は §5 が解こうとしている二択の対比そのものなので残す。見出し L108 と図タイトル（analysis.py L170「残差そのものが縮む」）の重なりを解消したい場合は、見出しではなく analysis.py L170 を「残差が縮む」に変える。

### OPTI-3
残差の t 依存性に一文触れる（1条件1秒未満なので安い）。抜き取り32条件では密度との順位相関は t=100 で +0.97、t=900 で +0.95、密度帯平均の単調増加はどの t でも成立するが、絶対値は t=100 と t=900 で約3倍動く。本文に載せるなら全135条件で測り直して数値を入れ替えること。CFG に触れるなら「本測定は条件側の枝のみを対象にしている」と書くのが正確で、「無条件側にも同じ残差が入る」は guess_mode=False では偽なので書かない。

### OPTI-4
§4.1 の環境依存性の主張を、既報の diffusers の不具合記録を [3] の隣に添えて補強する。レポートは独自性をどこでも主張していないので新規性の問題ではないが、Limitations の「本環境固有かもしれず、他環境での再現は未確認」を外部記録で挟むと弱点が減る。

### OPTI-5
exp3 完走後に走る exp4（条件とプロンプトの意味的衝突、20生成）と exp6（depth 条件での残差、拡散を回さない）は、必須項目のどれにも対応していない。時間が足りなければ実行を諦めてよい。exp6 は「Canny のみ」という Limitations を一点だけ緩められるので、余力があれば exp4 より優先する。

## 新規性の判定

3条件のうち、機構の新規性を主張できるのは §5 だけで、しかも現在の書き方ではそれが自壊している。

(1) attention slicing による沈黙した NaN（§4.1）＝既知。diffusers に既報の不具合が存在する。ただしレポートは独自性を一度も主張していない（本文に「独自」「新規」「初めて」「未報告」「発見」は0件、「既知」の出現はL64の1箇所のみで、そこは fp16 VAE 飽和という別の既知説明を退ける文脈で正しく使われている）。したがって新規性の瑕疵はない。価値は「公式文書が推奨する設定が、その文書が示す条件（RAM 64GB未満）に当てはまる環境で沈黙して壊れる」という組み合わせの提示と、異常検出を pipeline に組み込んだ実務判断にある。引用すべき出典：[3] の MPS ドキュメントは既に引いてある。既報の issue を添えると他環境での再現性の間接証拠になり、Limitations が強くなる（optional 扱い）。

(2) 条件マップの密度縮退（§4.2）＝因果説明は既知の実務知。既定閾値が入力のコントラストに対して高すぎるという指摘は公開議論に存在する（lllyasviel/ControlNet の Discussion で Canny の代替が論じられている）。新規なのは定量化の側（60条件の順位相関、密度帯ごとの整理、シード掃引による条件内分散の分離）。CHECKLIST L42 の枠組み修正（論文は Canny 閾値をランダム化して学習しているので、学習の不備ではなく条件付き生成の構造的限界である）は一次資料に基づく正しい位置づけで、これは保持すべき。ただし現状の中心的証拠が指標の定義上の帰結（F1=0.000）なので、must_fix 3 の差し替えを行わない限り「定量化した」とも言えない。

(3) conditioning_scale の既定値が最適でない（§4.3）＝主張として珍しくないが、レポートが述べているのは自分の仮説（最適値が密度で動く）が外れたことなので、新規性の争点にはならない。むしろ「掃引範囲が狭く、7件すべてが上限に張り付いた」という設計の不備の自己申告として読まれる。exp2b が完走すれば反転点の探索になり、単一指標の錯覚を自分で潰した記録として強くなる。

(4) 評価指標への帰属が欠けている。edge F1（再抽出エッジの F1、許容 σ=2 の膨張）は既に確立した設計で、ControlNet++（arXiv:2404.07987、Canny 条件の評価に F1 を使い、評価用閾値も (100,200)）と arXiv:2403.00467（σ=2 の max-pooling による許容）が該当する。引用しないことで、指標の妥当性を裏づける根拠を自ら捨てているうえ、独自考案のように見せている。ただし外部の F1 数値を並べて自分の値と直接比較するのは避ける（許容幅の定義が明示されていない／片側指標なので同一ではない）。

(5) §6 の改善案（密度適応 Canny ＋ 密度連動 scale）は auto-Canny も CLAHE も定型手法だが、arXiv:2403.00467（ACM MM 2024）とは狙いが異なる（あちらは不完全マスクの劣化推定）。§6 を書くときに差分を1文添えておくと安全。

(6) 唯一の新規性候補は §5 の残差 RMS プローブ（拡散を回さず ControlNet の注入残差を条件の密度に対して掃引する）。同種の測定の先行は見つかっていない。ただし現在その節の「決定的な証拠」が同一入力を2回測った恒等式に乗っており、機構の主張が空になっている。must_fix 4〜6 を直せば、この節が本レポート最大の貢献として立つ。逆に直さなければ、唯一の新規部分が最も脆い部分になる。

## 最大の弱点

最重要項目（Failure Case Analysis）の第一の論点と、機構節（§5）の決め手が、二つとも「測定結果」ではなく「指標の定義」と「入力の同一性」から自動的に出る値に乗っている。しかも両者が相互参照で支え合う構造になっているので、片方が崩れると両方同時に崩れる。

教員が最初に突くのは §5 L125 だと予想する。「エッジが消えた条件地図が注入する残差は 0.3846 で、何も描かれていない地図を与えたときの 0.4022 とほぼ一致する」——密度0とは Canny 出力に1画素もエッジが立たなかったことなので、その条件地図は参照に使った np.zeros((512,512)) とビット単位で同一の配列である。probe.py は seed と timestep も固定しているから、残差が一致するのは測定前から確定している。ここに「決定的なのは」「4.4%」という強調が付いているため、恒等式を経験的発見として提示した形になっている。差 0.0176 は、45行がプロンプトの違いだけで散る幅（SD 0.0150）の1.17倍でしかない。

二番目に突かれるのは §4.2 L92 の「edge F1 は例外なく 0.000、標準偏差も 0 であり、確率的な失敗ではなく決定的な破綻である」。pipeline.py の edge_f1 は条件エッジが空なら生成画像が何であっても 0 を返すので、これは破綻の測定ではない。実際に出力は壊れていない（生成側エッジ画素率 0.0074〜0.2539、CLIP も落ちていない）。壊れているのは制御であって生成ではないのに、「決定的な破綻」という語がそれを取り違えて見せている。

この二つが崩れると、L127 の「相関の観察ではなく、論文が与えた式から予測される挙動をそのまま測定したことになる」という自己評価が丸ごと下がる。禁止事項の「APIのみ呼び出して中身を理解していない」を回避するための唯一の柱が §5 なので、失点の幅が大きい。

救いは、これが実験の失敗ではなく書き方の失敗である点である。差し替え証拠は既存データで足りる。当方が pipeline.edge_f1 で実測した通り、密度0の生成を「本来指定したかった輪郭」（同じ入力の α=1.0 条件地図）と照合すると F1 0.2391 で、別入力どうしの偶然一致の床 0.2461 と区別できない（p=0.807）。正しく条件づけた場合は 0.7329。追加生成ゼロで、定義上のゼロに頼らずに出力側で同じ結論が言える。締切までに直せる範囲の問題であり、直せば §5 は本レポート最大の強みに戻る。

## 評価される点

誇張せずに言えば、評価される点は4つある。

第一に、§4.1 が「静かに壊れる失敗」を捕まえていること。例外も警告も出ず正常終了する黒画像から、VAE 復号前の潜在変数を取り出して NaN を確認し、fp32 VAE で復号し直しても消えないことから fp16 VAE の飽和という既知説明を退け、UNet 順伝播の破綻に絞り込んでいる。この切り分けの順序は正しく、しかも診断で終わらせずに pipeline.generate() へ NaN・単色の検出を組み込み、check_health.py で事後一括検査までしている。「コードを動かしただけ」との差がここで最も明確に付く。加えて、壊れる設定が公式文書の推奨で、しかもその文書が示す条件（RAM 64GB未満）に自分の環境が当てはまるという組み合わせは、失敗分析として報告価値がある。

第二に、一次資料での実装読解。論文の式を読んで満足せず diffusers 0.31.0 の controlnet.py を開き、解像度依存の重みが guess_mode 有効時のみであること（L844-851）を自分で確認している。この確認があるから、§5 で段ごとの残差を重み補正なしに比較する判断が根拠を持つ。必須項目の「手法理解」を、要約ではなく実装との突き合わせで満たしている。

第三に、出力側の相関で止めずに内部を測りに行った着想。密度と成績の相関は「条件が効かなくなった」とも「生成が難しくなった」とも読めるという問題設定が正しく、それを分けるために ControlNet の順伝播だけを1回走らせて残差の RMS を取るという設計は、拡散の全過程を回さないので1条件1秒未満で済む。生成に1枚100秒以上かかる環境で135条件を掃引できたのはこの設計のおかげで、計算資源の制約を手法の選択で回避している。恒等式の問題（must_fix 4〜6）を直せば、これが本レポート唯一の新規部分として残る。

第四に、事実確認の姿勢。論文に Limitations 節が置かれていないことを arXiv 版本文で確認して明記している。会議名を CVPR 2023 と誤提示されたのを Crossref・DBLP・Semantic Scholar の3ソースで訂正した記録があり、これは必須項目9（生成AI利用内容）に直結する具体例として質が高い。CHECKLIST L42 で、当初の「学習時に見ていない条件だから」という枠組みを、論文が Canny 閾値をランダム化して学習している事実に基づいて「条件付き生成の構造的限界」へ修正しているのも、一次資料が自分の仮説を否定したときに仮説を捨てられている例である。

誇張は避ける。個々の失敗条件は3つとも既知で、新規なのは定量化と統合の側である。そこはレポート自身も主張していないので、正直さとしては保たれている。

## 第6〜9節を書くときの注意

第6〜9節は、第1〜5節で見つかった4種類の罠を繰り返さないことが最優先。罠は (i) 定義上・構成上そうなる値を発見として書く、(ii) 独立でない標本で p 値を出す、(iii) 母集団の違う数値を比較して比を取る、(iv) 表や図と本文が食い違う。以下は節ごとの具体。

■第6節（改善前後比較）
・検定は α で層別し、各層内で画像を単位に3シードを平均してから Wilcoxon（must_fix 10）。α=0.3 と α=1.0 で符号が逆なので絶対に混ぜない。
・悪化を隠さない。実行途中のデータでは原画像側（α=1.0）が悪化しており（画像単位 3/11 改善、Δ中央値 −0.078）、これは「密度を上げれば良い」わけではないことの直接証拠で、§8 の主張（資源の価値はタスク構造との整合で決まる）と最も相性が良い。隠すと、改善が低コントラスト側だけで効く理由も説明できなくなる。
・n=12 で Wilcoxon は有意にならない見込みなので、p 値を見出しにしない。改善した画像数・Δ中央値・α別の符号・層内のばらつきを並べ、どちらの層も有意でないことを明記する。§4.2 で 0.3852 の帰属を誤ったのと同じく、統計値を出すときは必ず「どのデータのどの母集団か」を文中に書く。
・「追加の生成コストがゼロ」は事実だが売りにしすぎない。adaptive_canny の target=0.045 は手で決めた値で掃引していないので、その申告を §7 に回す。
・数値は exp3 完走後の CSV から再計算して確定させる。現在 69/90 行。

■第7節（Limitations）
今回の指摘から、正直に書ける項目が増えた。CHECKLIST L62-66 の5項目に加えて次を入れる。
・edge F1 は条件エッジが空の帯で定義上0に固定され、分散も失う。この帯の評価は残差測定に依存している（§4.2 と §5 の関係を自分で開示する）。
・許容幅の感度（0px 0.298／2px 0.829／3px 0.869）。結論の向きは保たれるが絶対値は指標設計に依存し、他文献の F1 と直接比較はできない。
・密度帯の境界は記述のために手で切った。非恣意的な境界は密度がちょうど0だけである。
・残差は t=500 の1点・単一シード・条件側の枝のみの測定で、絶対値は t で約3倍動く（順位は動かない）。
・残差はエッジ画素率だけの関数ではなく配置に依存する（同密度で3.3倍の幅）。密度は代理変数である。
・最適 scale は右側打ち切りで、exp2b が間に合わなければ「1.3 以上」としか言えない。
・adaptive_canny の目標密度 0.045 を掃引していない。しかも本実験では 0.05 を超えても edge F1 の飽和が見えず最高値は密度 0.1316 で出ているので、この目標は保守的すぎる可能性がある。
・条件内ばらつきと密度の関係は方向が一貫するにとどまり検出力がない（ρ=−0.70、p=0.19、n=5）。
書き方の注意：Limitations は弱点の列挙ではなく「どこまでなら信じてよいか」の線引きとして書く。§4.2 の帯別SDのように、何を測っていて何を測っていないか（生成の再現性ではなく画像間の水準差）を分けて書けている箇所が既にあるので、その調子で揃える。

■第8節（考察）
・「密度と scale が同じ資源に対する二つの支払い方」を主張するなら、根拠を exp2 に置けない。密度と Δ(1.0→1.3) の相関は ρ=−0.393、p=0.383 で存在しない。根拠は §4.2（密度と構造整合、ρ=+0.819／密度0除外で +0.577）と §5（残差の減衰）側に置く。ここを間違えると、§4.3 で「予想が外れた」と正直に書いた効果が消える。
・研究テーマとの接続は1段落に留め、こじつけない。接続点として自然なのは、残差が画素数ではなく配置に依存するという実測（同じ密度でも3.3倍の幅）と、改善が低コントラスト側だけで効いて原画像側では代償を払うという非対称である。どちらも「資源の量ではなく構造との整合で符号と大きさが決まる」という自分の芯に、修飾なしで対応する。逆に「計算量を増やすほど良いとは限らない」を一般論として述べるだけなら書かない方がよい。
・§5 を直した後は、この節の結論を「条件という資源は空間的な指定を運ぶ帯域を持ち、その帯域が尽きると注入は続くのに情報だけが失われる」という形で書ける。「残差が消える」ではなく「入力依存成分が消えて定数項に潰れる」が正確な機構であることを §5 と揃える。

■第9節（生成AI利用）
・使用ツール／用途／自分で判断・修正した箇所を分けて書く。用途は正直に列挙する（コード生成、実験の自動化、文献の探索、文章の下書き）。
・具体例が価値を持つ節なので、記録済みの CVPR→ICCV 訂正（Crossref・DBLP・Semantic Scholar の3ソースで確認）に加えて、今回の照合で見つかった修正も書ける。すなわち、実行環境の生成速度とモデル容量に自分の実測と合わない数値が入っていたのを、results/ 配下の全CSV（sec 列 129件）と重みファイルのヘッダから再計算して直した、という記録。AI の出力を検証せずに通すと数値が入るという具体例になる。ただし盛らない。実際に自分が直した範囲だけを書く。
・「AI生成文の丸写し」の回避を主張するなら、地の文の太字全廃（should_fix 1）を先に済ませておく。文中を太字で拾い上げる体裁は生成モデルの出力の最も分かりやすい癖なので、残したまま「丸写しではない」と書くと見た目が主張と食い違う。

■全節共通
・新しく書いた数値は必ず CSV から再計算して確認する。第1〜5節で一次データと合わなかったのは §3 の3値と §4.2 の 0.3852 で、いずれも「実測」と書いてある位置だった。
・図を貼るなら図題・軸ラベルと本文の用語・結論の一致を最後に目で確認する（fig2 が「ρ=+nan」と本文と逆の図題を持っていた前例がある）。
・最後に PDF を再生成し、4〜8ページに収まっているか、`**` の変換漏れがないか、図が該当節の近くに落ちているかを確認する。現行PDFは 07:12 のビルドで本文より古い。
