# 設問対応チェックリスト（映像メディア学 レポート課題）

最終更新 2026-07-31 05:30。**このファイルが現状の正。**

- 締切: **2026-07-31(金) 23:59 JST**（課題PDFの「2027/7/31(金)」は曜日が土曜で不整合。2026/7/31 が金曜のため 2026 年と判断）
- 提出: UTOL。メール送付は不可

## 提出物

| | 状態 |
|---|---|
| レポート | `48266454_新井滉明_映像メディア学_レポート.pdf` **8ページ**・欠字ゼロ・図2枚 |
| ソースコード | https://github.com/komeiall14/controlnet-failure-analysis （公開） |
| 生成AI利用ログ | レポート第9節（使用したAI／用途／自分で修正・判断した箇所） |

## 必須10項目

| # | 項目 | 対応する節 |
|---|---|---|
| 1 | 氏名・学籍番号・専攻・研究室・研究テーマ | 1ページ目 |
| 2 | 対象論文概要 | §1 |
| 3 | 手法理解 | §2 |
| 4 | 実行環境 | §3 |
| 5 | **Failure Case Analysis（最重要）** | §4（3本） |
| 6 | 改善内容 | §4.1 と §6（2件） |
| 7 | 改善前後比較 | §6 |
| 8 | Limitations | §7（5点） |
| 9 | 生成AI利用内容 | §9 |
| 10 | 考察 | §8 |
| + | GitHub URL | 1ページ目 |

## Failure Case（要求は2種類以上、本レポートは3本）

1. **推奨されている省メモリ設定が沈黙したまま出力を壊す** — `enable_attention_slicing()` で UNet 順伝播に NaN。例外も警告も出ない。**原因は MPS の `baddbmm` が `beta=0` の規約を守らず、未初期化の加算元を読むこと**（`exp11`）。バッファには fp16 で 90.3% の非有限値が入っている（`exp10`）。加算元を 0 で初期化すれば直り、20ステップの本番条件で slicing 無効時と画素差 0.18（`exp12`）
2. **条件地図の密度が下がると制御が消える** — 60条件中15条件でエッジ画素率が 0。本来の輪郭との一致が 0.7329 → 0.2391 で偶然の水準と区別できない。壊れているのは制御であって生成ではない
3. **推論時の既定値が構造整合に対して弱い** — 既定 1.0 が最適な入力は7件中0件。§8 でプロンプト追従と併せると妥当な妥協点だと判明

**機構の直接証拠**: UNet へ注入される残差を1回の順伝播で測定。密度が正の領域で ρ=+0.669

## 改善（2件）

1. 加算元を 0 で初期化する修正（§4.1）。20ステップで5枚すべて正常、edge F1 は slicing 無効時と一致
2. 目標密度への二分探索＋CLAHE、密度連動 scale（§6）。生成コストは増えない
   - 開発15画像: α=0.3 で 12/15・共通参照 Δ+0.0665・p=0.055
   - **開発に使っていない11画像: 10/11・Δ+0.0788・p=0.010**
   - 無条件適用は原画像側で有意に害（p=0.024）。適用条件で Δ0.0000 に戻る
   - プロンプト追従（CLIP）は有意に動かない

## 自動検査（すべて終了コード0）

| スクリプト | 保証するもの |
|---|---|
| `stats_report.py` | 本文が挙げる統計量を節ごとに再現する |
| `audit_numbers.py` | 本文の数値に出所がある（データ由来の出所不明ゼロ） |
| `audit_flow.py` | 論理のつながり（予告の回収・用語の初出・指示語・記号の定義） |
| `audit_consistency.py` | 提出物どうしの食い違い（画像数・共通の数値・ファイル一覧・名指し） |
| `make_pdf.py` | 欠字・申し送りの混入・節間の矛盾。上2つも内部で実行する |

## 禁止事項の自己点検

| 禁止 | 回避状況 |
|---|---|
| コードを動かしただけ | 失敗条件3本＋機構の測定＋改善2件＋統計検定＋held-out 検証 |
| failure analysis なし | 最重要項目として3本。いずれも「なぜ壊れるか」まで実験で示した |
| AI生成文の丸写し | 検査を4本用意し、差し戻した箇所を §9 に列挙 |
| 実験なしの感想文 | 全主張に実測データ |
| 著作権違反データ利用 | skimage.data 同梱＋自作合成のみ。図1の camera は CC0（0.18 で差し替え済み） |
| API のみ呼び出して中身を理解していない | 残差を直接測定し、diffusers の実装を読んで欠陥を特定した |

## 経緯

敵対的レビューを6回。確定した誤りは延べ100件超。主なものは
指標の循環（密度0で F1=0 は定義から出る値）、恒等式の比較、実装批判の撤回、
検定単位の水増し、改善前後で参照地図が違った点、held-out に開発側と
同一の画像（`skimage.data.cat` は `chelsea` のエイリアス）が混入していた件。
